#include <stdio.h>

/**
 * main - function that prints a string of characters using the printf function
 *
 * Return: returns zero after executing
 */

int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
